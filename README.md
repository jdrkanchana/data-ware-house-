# data-ware-house-
sample project for data ware house
creating customer dimension table, product dimension table, stores dimension table and sales person dimension table and inserted values

to update git : git status
to add file to git : add <file name>
to commit git : git commit -m "<what you have done in last change>"
to push to github : git push -u origin master
